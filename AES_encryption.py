from utils import split_blocks, XOR




def encrypt(plaintext, key)->str:
    '''First step is splitting plaintext into blocks of size 128'''
    blocks = split_blocks(plaintext, 128)
    
    return None

def encryption_round(plain_block, round_key):
    
    return None



def decrypt(ciphertext, key)->str:
    return None
